# 點歌系統・怎麼優化

目前系統已經能穩定跑了，這份文件記錄還可以再優化的方向，供之後挑著做。不是待辦清單，是選單。

## 一、畫質

- 現有 9 首歌來源大多只有 480p（部分 640×480、320×240），已用 `libx264 crf17` 重新編碼，接近來源畫質上限
- 若要真正變清楚，得找 720p/1080p 高清來源重新下載覆蓋。**之前討論過、你先擱置了**：樹莓派 5 對 1080p H.264 硬解沒把握順不順，需要先拿一首實測（燒錄時間也會拉長到 10 分鐘+）
- 之後若要做，建議先測一首確認不卡再決定要不要全部換

## 二、穩定性 / 部署安全

- **服務用的是 Flask 內建開發伺服器**（log 裡一直有 `WARNING: This is a development server`），正式環境建議換成 gunicorn/waitress 之類的 production WSGI server，比較耐用、不會被單一慢請求卡住整個服務
- **部署重啟要避開下載/燒錄中**：`systemctl restart jukebox` 會把正在跑的 ffmpeg（下載後燒 QR、伴奏合成）一起殺掉，留下半成品壞檔（今天實際發生過一次）。之後部署前應該先 `curl /api/download_progress` 確認 `busy:false` 再重啟，或乾脆在 app.py 加個「有任務在跑就延後重啟」的保護
- mpv 卡死自癒機制已有（12 秒沒動靜自動重播），但沒有上限次數，理論上可能無限重試耗資源；可以加個「連續失敗 N 次就跳下一首並通知」的保護

## 三、安全性

- 目前所有 API（點歌、刪除、下載、上傳伴奏…）對同 WiFi 下任何手機完全開放，沒有密碼或驗證。正常 KTV 場景可能沒差，但若擔心有人惡意洗歌單/刪歌/塞一堆下載，可以加個簡單的房間 PIN 碼或限流
- `/api/download_url` 沒有下載內容/來源限制，理論上任何人都能透過這台機器的頻寬下載任意 YouTube 影片，長期可能被濫用當下載機；可考慮限制每小時下載次數

## 四、使用體驗

- 今天已加：按鈕按下變色回饋、操作成功/失敗 toast 提示、搜尋結果不再被截斷
- 可以再做：
  - 下載/燒錄進度加個可視化進度條（目前燒 QR 階段只顯示文字，沒有百分比）
  - 網路斷線/API 逾時時前端目前多半是 silent fail 或籠統的「⚠️ 網路錯誤」，可以更明確提示（例如區分「連不上樹莓派」跟「樹莓派處理逾時」）
  - 點歌後可以讓前端顯示「還要等幾首才輪到你」的預估時間

## 五、維運

- **磁碟自動清理**：已有邏輯（用量超過 20GB 開始清到 17GB，10 天內播過/下載的不刪），可以定期確認邏輯還符合實際使用量
- **開機画面**：已修好不進桌面，但 `/etc/xdg/labwc/autostart` 是系統檔案，未來系統更新（`apt upgrade`）有可能被套件覆蓋還原，需要留意
- 目前只有你手動 ssh 進去看 log/狀態，沒有任何監控告警；可以考慮一個簡單的健康檢查腳本（服務掛了/磁碟滿了會透過某種方式通知你）

## 六、開發流程

- 專案目前只有本機一份 + 樹莓派一份，靠你手動 scp 同步，沒有版本控制備份到雲端（`git remote` 還沒設定）。建議至少建一個私有 GitHub repo 定期 push，避免本機掛了整個專案歷史都不見
- 本機還躺著一份舊的 `点歌系统.tar.gz`（9/3 的舊備份），沒在用，可以清掉避免混淆
